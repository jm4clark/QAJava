import java.util.Scanner;

public class Runner {
	public static void main(String[] args) {
		Scanner s = new Scanner(System.in);

		String input = s.nextLine();

		String[] numbers = {"zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine" };

		String[] tens = { "ten", "twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety" };

		for (int i = 0; i < 10; i++) {
			//System.out.println(numbers[i]);
		}

		String s1 = "";

		for (int i = 0; i < input.length(); i++) {
			if (i != 0)
				s1 += " ";

			char theChar = input.charAt(i);
			if(Character.getNumericValue(theChar <10 ){
			   s1+= numbers[Character.getNumericValue(theChar)];
			}
			
			/*switch (theChar) {
			case '0':
				s1 += numbers[0];
				break;
			case '1':
				s1 += numbers[1];
				break;
			case '2':
				s1 += numbers[1];
				break;
			case '3':
				s1 += numbers[3];
				break;
			case '4':
				s1 += numbers[4];
				break;
			case '5':
				s1 += numbers[5];
				break;
			case '6':
				s1 += numbers[6];
				break;
			case '7':
				s1 += "seven";
				break;
			case '8':
				s1 += "eight";
				break;
			case '9':
				s1 += "nine";
				break;
			}*/
		}
		
		System.out.println(s1);

		s.close();
	}
}
